pandas>=2.0.0
numpy>=1.24.0
PyYAML>=6.0
# Optional: real historical data from Yahoo Finance for backtesting.
yfinance>=0.2.40
# MetaTrader5 is Windows-only; install separately if running live trading on Windows:
#   pip install MetaTrader5
